// How to configure the sample test application

Run powershell or CMD here.

Run 'npm install'

once that is completed run 'npm start' start application. make sure the port is 3000.

// Setup Json server

Run 'npm install -g json-server'

once completed run 'json-server --port 3001 --watch ./data/data.json'